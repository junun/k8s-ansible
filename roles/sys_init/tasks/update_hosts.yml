- name: Apply new hosts file
  shell: |
    sed -i '/#BEGIN --generated by kubernetes/,/#END --generated by kubernetes/d' /etc/hosts
    cat /tmp/hosts >> /etc/hosts
  args:
    executable: /bin/bash